function displayMessage() {
  console.log(
    "To destructure an array in JavaScript, place variable names inside square brackets on the left side of the assignment operator, matching the positions of the items in the array on the right side, like: const [firstItem, secondItem] = myArray;",
  );
}

export const results = [5, displayMessage];
