import { props } from "./props.js";

// Unpacking properties directly into standalone variables
const { title, message } = props;

// Displaying the values of the newly created variables
console.log(title);
console.log(message);
