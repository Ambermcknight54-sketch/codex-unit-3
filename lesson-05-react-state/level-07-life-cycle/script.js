// 7 & 8. An array of messages explaining the React component lifecycle in unique words
const lifecycleMessages = [
  "The journey of a React component is split into three main parts called mounting, updating, and unmounting.",
  "Mounting is the initial phase where a brand new component gets created and inserted directly onto your screen's view.",
  "Updating happens automatically whenever a component's internal state switches or when it receives brand new props from its parent.",
  "Unmounting is the final cleanup stage where the component is completely pulled away and destroyed from the webpage.",
];

// 9. Display the array in the terminal
console.log(lifecycleMessages);
