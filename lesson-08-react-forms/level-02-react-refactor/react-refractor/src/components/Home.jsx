export function Home() {
  return (
    <main>
      <h1>Home Page</h1>
      <p>
        Identify: Extract self-contained UI sections or repeating elements into
        their own files. Isolate Dependencies: Determine which variables, state,
        or functions the new JSX relies on.
      </p>
    </main>
  );
}
