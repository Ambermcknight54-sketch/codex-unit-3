export function Home() {
  return (
    <main>
      <h1>Home Page</h1>
      <p>
        To join nested data using Prisma in React, pass an include object in
        your findMany query for example, including reviews and nested users.
        Once fetched and stored in state, pass the nested arrays down to child
        components as props and map over them using Fragment, dt, and lt;dd&gt
        tags to display the results.
      </p>
    </main>
  );
}
