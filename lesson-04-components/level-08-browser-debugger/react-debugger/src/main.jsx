function Main() {
  const message =
    "To start the debugger for React projects: launch your Vite dev server in the terminal, open the VS Code Run and Debug panel, select your custom 'React Debugger' launch configuration, and click the play button to launch the browser connection.";

  return (
    <main>
      <h2>Application Workspace</h2>
      <p>{message}</p>
    </main>
  );
}

export default Main;
