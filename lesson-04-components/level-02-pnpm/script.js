console.log(
  "pnpm saves disk space and time by sharing files between projects!",
);
console.log(
  "Instead of giving every project its own copy of a package, // script.js",
);
console.log(
  "pnpm saves disk space. Instead of giving every project its own copy of a package, pnpm stores them one folder on my computer.",
);
console.log("When you download once installation is faster.");
