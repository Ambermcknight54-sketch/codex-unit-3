import chalk from "chalk";

console.log(chalk.cyan("Success! Chalk is installed and working."));
console.log(
  "dependencies are external code libraries, tools, or packages that your project relies on to function properly.",
);
