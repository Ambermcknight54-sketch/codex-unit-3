export function Home() {
  return (
    <main>
      <p>React SPA-Template</p>
      <p>
        A SPA (Single Page Application) Template is a pre-configured starting
        codebas to build web applications. Rather than loading entirely new HTML
        pages from a server every time you click a link (like traditional
        multi-page websites), a Single Page Application loads a single HTML page
        and updates the content as the user interacts with it
      </p>
    </main>
  );
}
