export function Home() {
  return (
    <main>
      <h1>Home Page</h1>
      <p>
        A SPA ,Single Page Application, is a pre-made template starting point
        for building websites faster, rather than a traditional multi-page
        website. Instead of reloading the entire web page every time a user
        clicks a link a SPA loads a single HTML file once.
      </p>
    </main>
  );
}
