console.log("To run a JavaScript file in your terminal, follow these steps:");
console.log(
  "1. Open your terminal (like Git Bash or the built-in VS Code terminal).",
);
console.log(
  "2. Use the 'cd' command followed by the folder path to navigate to where this file is saved.",
);
console.log("3. Type 'node script' (or 'node script.js') and press Enter!");
